## Change

Describe the problem and resulting behavior.

## Verification

- [ ] Offline tests pass (`python -m unittest discover -s tests -v`).
- [ ] Changed UI was checked in a browser.
- [ ] No keys, local configuration, weights, executable binaries, or personal paths are included.
- [ ] Provider changes include contract and lifecycle tests.
- [ ] Model outputs, planner overrides, and local/cloud timings remain clearly distinguished.
